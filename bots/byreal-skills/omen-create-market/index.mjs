#!/usr/bin/env node
// TODO(team): wire to MarketFactory.createMarket using ethers, reading inputs from process.env
//             (Byreal CLI passes inputs as env vars matching the input names).
console.log("[omen-create-market] TODO(team): implement createMarket call");
